#!/bin/bash

`rospack find roslib`/scripts/pythontest
if [ $? -eq 0 ]; then
  `rospack find roslib`/scripts/gensrv.py $*
else
  for i in `rospack langs`; do
    lang=${i:3}
    `rospack find $i`/scripts/gensrv_$lang $*
  done
fi
