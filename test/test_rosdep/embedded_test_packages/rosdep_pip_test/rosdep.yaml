pip-based-install:
  ubuntu: 
    pip:
      packages: [ pycurl ]
pip-versioned-install:
  ubuntu: 
    lucid: 
      pip:
        packages: [ pycurl ]

pip-based-install-dependent:
  ubuntu: 
    pip:
      packages: [ pycurl ]
      depends: [ pip-based-install]